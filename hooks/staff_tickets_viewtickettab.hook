<?php

	/**
	 * Call the cm_TFS app once the ticket tabs have been loaded
	 */
	if (SWIFT_App::IsInstalled('cm_TFS'))
	{
		$this->Load->Controller('cm_TFS','cm_TFS')->Load->Method('loadTab', $_SWIFT_TicketObject);
		
	}
?>